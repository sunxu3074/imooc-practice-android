<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.isunxu.imooc_practice_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.ViewPagerBringTabActivity"/>
        <activity android:name=".activity.FragmentBringTabActivity"/>
        <activity android:name=".activity.FragmentPagerAdapterAvtivity"/>
        <activity android:name=".activity.TabLayoutBringTabActivity"></activity>
    </application>

</manifest>
